import { AutocompleteoffDirective } from './autocompleteoff.directive';

describe('AutocompleteoffDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocompleteoffDirective();
    expect(directive).toBeTruthy();
  });
});
